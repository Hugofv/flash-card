import React from 'react';
import { StartButton, Container, Input } from './styles';

const Home = () => {

  return (
    <Container>
      <Input />
      <StartButton>
        Start
      </StartButton>
    </Container>
  );
}

export default Home;
