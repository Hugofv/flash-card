import React from 'react';
import { StartButton } from './styles';

const Home = () => {

  return (
    <div>
      <StartButton>
        Start
      </StartButton>
    </div>
  );
}

export default Home;
